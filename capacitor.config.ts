import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppPrecioLuz',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
